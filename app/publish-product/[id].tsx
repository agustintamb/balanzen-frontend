import PublishProductScreen from "@/components/PublishProductScreen";

// Ruta apilada (sin tab bar) para editar una publicación existente.
// El hook detecta el modo edición a partir del param `id`.
export default PublishProductScreen;
