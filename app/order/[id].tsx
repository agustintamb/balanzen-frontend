import OrderDetailScreen from "@/components/OrderDetailScreen";

// Ruta raíz apilada (sin tab bar). Detalle de una orden leída por `order_id`
// (GET /orders/{id} → publicación completa + consumidor). Acciones según rol.
export default OrderDetailScreen;
