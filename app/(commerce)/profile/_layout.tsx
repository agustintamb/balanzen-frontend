import { Stack } from "expo-router";

const ProfileLayout = () => (
  <Stack
    screenOptions={{
      headerShown: false,
      contentStyle: { backgroundColor: "#F1EFE8" },
    }}
  />
);

export default ProfileLayout;
