import { Stack } from "expo-router";

const PublishLayout = () => (
  <Stack
    screenOptions={{
      headerShown: false,
      contentStyle: { backgroundColor: "#F1EFE8" },
    }}
  />
);

export default PublishLayout;
