import PublishProductScreen from "@/components/PublishProductScreen";

// Tab "Publicar" (FAB). Renderiza la pantalla de crear publicación. El tab bar
// se oculta mientras este tab está activo (ver components/TabBar). La edición
// reutiliza la misma pantalla vía la ruta apilada /publish-product/[id].
export default PublishProductScreen;
