import PublicationDetailScreen from "@/components/PublicationDetailScreen";

// Ruta raíz apilada (sin tab bar). Detalle de una publicación leída por
// `publication_id`. El hook decide las acciones según estado y rol.
export default PublicationDetailScreen;
